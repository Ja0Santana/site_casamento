# Configurações Gerais
spring.application.name=Site-Casamento
server.port=${PORT:8080}
server.address=0.0.0.0

# Mercado Pago (Mantenha as variáveis no Railway)
mercado-pago.access-token=${MERCADO_PAGO_ACCESS_TOKEN}
mercado-pago.public-key=${MP_PUBLIC_KEY}

# Banco de Dados (Conexão automática Railway)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/db_convidados}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:b2a7c9o3}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# --- CONFIGURAÇÕES DE E-MAIL (RESEND API) ---
# Substitui completamente o JavaMailSender antigo
resend.api.key=${RESEND_API_KEY}
resend.email.from="Elen e Joao Paulo <contato@casamentoelenejoaopaulo.men>"

# Destinatário das notificações do casal
EMAIL_CASAL=${EMAIL_CASAL}

# Log para debug (Opcional - Ajuda a monitorar o envio no console do Railway)
logging.level.com.joaopaulo.Site_Casamento.service=INFO
